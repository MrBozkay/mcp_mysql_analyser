name: Create Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Use Node.js 20.x
      uses: actions/setup-node@v4
      with:
        node-version: 20.x
        cache: 'npm'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Build project
      run: npm run build
      
    - name: Create package
      run: npm pack
      
    - name: Get package info
      id: package
      run: |
        echo "name=$(node -p "require('./package.json').name")" >> $GITHUB_OUTPUT
        echo "version=$(node -p "require('./package.json').version")" >> $GITHUB_OUTPUT
        
    - name: Create Release
      uses: actions/create-release@v1
      id: create_release
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ github.ref_name }}
        release_name: ${{ steps.package.outputs.name }} ${{ github.ref_name }}
        body: |
          ## MCP MySQL Analyzer ${{ github.ref_name }}
          
          ### 🚀 Multi-Platform MCP Support
          
          This release adds comprehensive support for multiple MCP-compatible platforms:
          
          - 🎯 **Kiro IDE** - Native MCP support
          - 💻 **VSCode** - With MCP extension
          - 🤖 **Claude Desktop** - Anthropic's official app
          - ✨ **Google Gemini** - MCP protocol support
          - 🔧 **Cursor IDE** - AI code editor
          - 📱 **Any MCP Client** - Universal compatibility
          
          ### 📦 Easy Installation
          
          **Global NPM Installation:**
          ```bash
          npm install -g @mrbozkay/mcp_mysql_analyser
          ```
          
          **GitHub Installation:**
          ```bash
          git clone https://github.com/MrBozkay/mcp_mysql_analyser.git
          cd mcp_mysql_analyser
          ./install-global.sh  # Linux/Mac
          .\install-global.ps1  # Windows
          ```
          
          ### ⚡ Quick Setup
          
          ```bash
          # Set environment variables
          export MYSQL_HOST=localhost
          export MYSQL_USER=your_username
          export MYSQL_PASSWORD=your_password
          export MYSQL_DB=your_database
          
          # Configure for your platform
          mcp-mysql-analyzer setup  # Kiro IDE
          mcp-mysql-analyzer --help # See all options
          ```
          
          ### 🔧 Platform Configurations
          
          Each platform has specific configuration requirements. See the [README](https://github.com/MrBozkay/mcp_mysql_analyser#platform-configurations) for detailed setup instructions.
          
          ### 🧪 What's New
          
          - Global CLI wrapper for easy installation
          - Platform-specific configuration examples
          - Automated installation scripts
          - Property-based testing with fast-check
          - Enhanced error handling and validation
          - Comprehensive documentation
          
          ### 📖 Documentation
          
          Full documentation available at: https://github.com/MrBozkay/mcp_mysql_analyser#readme
        draft: false
        prerelease: false
        
    - name: Upload Release Asset
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: ./mrbozkay-mcp_mysql_analyser-${{ steps.package.outputs.version }}.tgz
        asset_name: mrbozkay-mcp_mysql_analyser-${{ steps.package.outputs.version }}.tgz
        asset_content_type: application/gzip